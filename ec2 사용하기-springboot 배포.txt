** ec2 생성하기 **
1. 계정 만들기 
- 이메일 주소(giyong2kim@nate.com), 계정이름 :  이메일 인증(인증문자)
- 인증후 비밀번호 입력(hill##0116)
- 회원 가입 진행후(영어로 작성)
- 해외 결제가 가능한 카드 입력(100원 결재후 환불됨)

2. aws 프리터어
  - 기본 지원 무료 - ec2 750시간 무료
3. ec2 생성
  - 인스턴스 시작
  - 서버 이름(web_server)
  - ubuntu(t2.micro)
  - 키페어 이름: my-ec2-key.pem
  - 인스턴스 실행
  - 보안그룹:  TCP 8080 포트 열기
  - 퍼블릭 도메인 : 브라우저 접속

*** net-tools 설치 : 네트워크 상태 표시
sudo apt install net-tools
netstat -nltp 

*** jdk21 설치 및 jar 파일 배포
$ sudo apt update
$ apt-cache search openjdk (검색)
$ apt-cache search openjdk | grep openjdk-21(상세 검색)
$ sudo apt install openjdk-21-jdk (설치)
$ java -version(설치 확인)

** jar 파일 업로드 명령
scp -i [키파일.pem] [업로드할_파일명] [EC2_계정명]@[EC2_IP]:[저장할_경로]
예) D:/aws-ec2 폴더안에 pem과 jar파일 준비, EC2 퍼블릭 IP - 52.62.171.176
D:\aws-ec2>scp -i "my-ec2-key.pem" "test_app.jar" ubuntu@52.62.171.176:/home/ubuntu/

$ ls - 파일 확인
$ java -jar test_app.jar (배포)
$ nohup java -jar test_app.jar & (nuhup - SSH 종료되어도 계속 실행됨)
브라우저 접속 - http://52.62.171.176:8080
** 프로세스 중지 방법
1. kill -9 PID
2. pkill -f test_app.jar

*** RDS로 mysql 연결
 - RDS 대시보드 -> 데이터베이스 생성
- MySQL 
- 프리티어
- DB인스턴스: codingOn
- hostname(엔드포인트):codingon.c3ucywos8r13.ap-southeast-2.rds.amazonaws.com
- user: space
- 비번(8자이상-space1234)
- 연결 : EC2 컴퓨팅 리소스에 연결안함, 퍼블릭엑세스: 예
- 보안 그룹:  sg-fdfdf-default > 인바운드 -규칙 추가 >mysql 3306
- 5분 정도 시간 소요후 db 인스턴스 생성됨


** workbench 설정
- hostname:spacecl.crqmsmmq203o.ap-southeast-2.rds.amazonaws.com
- port: 3306
- user:space
- password:space1234

-db 생성 
create database spacedb;(이미 권한 부여됨)
use spacedb;

스프링부트 - applicaton.property
# DataSource 설정 - mysql(aws-rds)
spring.datasource.driver-class-name=com.mysql.cj.jdbc.Driver
spring.datasource.url=jdbc:mysql://spacecl.crqmsmmq203o.ap-southeast-2.rds.amazonaws.com:3306/spacedb?serverTimezone=Asia/Seoul 
spring.datasource.username=space
spring.datasource.hikari.password=space1234
